import { style, styleVariants } from "@vanilla-extract/css";

export const productType = style({
    position:'absolute',
    top:'10px',
    right:'10px',
    border:'0',
    background:'transparent',
})

// const variants = {
//     transparentType : {
//         
//     }
// }

// export const bookmarkVariants = styleVariants(variants);