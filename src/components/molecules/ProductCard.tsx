import React from 'react'

interface ProductItemProps {
    top?:ReactNode;
    title?:string;
}

const ProductCard = ({title]}) => {
  return (
    <div>

    </div>
  )
}

export default ProductCard